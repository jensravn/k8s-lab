server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # Serve the SPA for unknown paths so client-side routing works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
