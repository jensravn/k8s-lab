apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  # ClusterIP, not LoadBalancer: the Gateway owns the only public IP, and an
  # HTTPRoute sends this hostname's traffic here.
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 8080
