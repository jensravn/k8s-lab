apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  # ClusterIP: reachable only inside the cluster. Nothing from outside gets
  # here until something public is put in front of it.
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 8080
